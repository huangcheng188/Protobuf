syntax = "proto3";

//对应的启动模块的类型
enum ApplicationResponseType {
    SWITCHPP_TRANSLATION_MASTER_SIMULTANEOUS_RESPONSE = 0;       // 同传翻译
    SWITCHPP_TRANSLATION_MASTER_DIALOGUE_RESPONSE = 1;           // 对话翻译
    SWITCHPP_SPEECH_INSCRIPTION_RESPONSE = 2;                    // 演讲题词
    SWITCHPP_SOUND_RECORD_RESPONSE = 3;                          // 超能录音
    SWITCHPP_AI_ASSISTANT_RESPONSE = 4;                          // AI助手
    SWITCHPP_CONVERSATION_RECORD_RESPONSE = 5;                   // 轻语速记
    SWITCHPP_AR_FACE_RECOGNITION_RESPONSE = 6;                   // AR识人
    SWITCHPP_IMG_MANAGER_RESPONSE = 7;                           // 图像和相册管理
    SWITCHPP_VOICE_SUBTITLE_RESPONSE = 8;                        // 语音字幕
}

//手机app启动对应模块的成功失败状态
message StartAppResult {
    bool is_success_start = 1;    //app是否成功启动了模块
    uint32 error_code = 2;       //app启动失败时候的code
    bytes error_msg = 3;         //app启动失败时候的msg
}

message SwitchApplicationResponse {
    ApplicationResponseType response_type = 1;
    StartAppResult start_app_result = 2;
}